<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.infowithvijay.triviaquizapp2">

    <application
        android:name=".utils.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activites.ScoreActivity"></activity>
        <activity android:name=".activites.GameOverScreen" />
        <activity android:name=".activites.CategoryActivity" />
        <activity android:name=".activites.Settings" />
        <activity android:name=".activites.QuizActivity2" />
        <activity android:name=".activites.HomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>